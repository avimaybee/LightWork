/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind v4: prefer CSS-first theming via `@theme` in index.css.
  content: ["./index.html", "./**/*.{js,ts,jsx,tsx}"],
  plugins: [],
}
